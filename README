Installation:
  git clone https://github.com/nikulabs/dotvim.git ~/.vim

Create Symlinks:
  ln -s ~/.vim/vimrc ~/.vimrc

Afterwards launch vim and run ":PluginInstall" to get access to the plugins referenced.
