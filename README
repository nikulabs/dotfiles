Installation:
  git clone git://github.com/nikulabs/dotvim.git ~/.vim

Create Symlinks:
  ln -s ~/.vim/vimrc ~/.vimrc

Switch to the '~/.vim' directory, and fetch submodules

  cd ~/.vim
  git submodule init
  git submodule update
